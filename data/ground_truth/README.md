# Ground Truth Datasets

This directory holds physical laser disto, steel tape measurement audits, and certified defect annotations used for benchmark accuracy scoring.

When physical measurements are absent, benchmark gates strictly report `NOT_EVALUABLE` / `PENDING_GT`.
