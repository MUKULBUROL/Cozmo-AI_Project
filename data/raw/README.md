# Raw Capture Data

This directory serves as the local drop location for uncompressed or raw sensor capture archives (LiDAR bundles, MP4 videos, and photo sets) during offline development and CLI execution.

The pipeline searches `data/raw`, `data/`, and `sample data/` when resolving local scan archives.
